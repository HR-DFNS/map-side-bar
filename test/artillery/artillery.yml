config:
  target: "http://localhost:3003"
  phases:
    - duration: 8
      arrivalRate: 20
  processor: "./artillery_functions.js"

scenarios:
  - name: "Bombard the page!"
    flow:
      - loop:
        - function: "generateRandomId"
        - get:
            url: "/restaurants/{{ id }}/"
        # - log: "sent request with {{ id }}"
        count: 125
